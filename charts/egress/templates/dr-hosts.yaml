{{ if and .Values.tlsOrigination (eq .Values.protocol "tls") }}
{{ range .Values.hosts }}
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: {{ trimPrefix "*." . | quote }}
spec:
  host: {{ . | quote }}
  subsets:
  - name: tls-origination
    trafficPolicy: {{ toYaml $.Values.trafficPolicy | nindent 6 }}
      portLevelSettings:
      - port:
          number: {{ $.Values.port }}
        tls:
          mode: SIMPLE
{{ toYaml $.Values.portLevelSetting | nindent 8 }}
  - name: {{ include "name" $ }}
    trafficPolicy: {{ toYaml $.Values.trafficPolicy | nindent 6 }}
      portLevelSettings:
      - port:
          number: {{ $.Values.port }}
        tls:
          mode: DISABLE
{{ toYaml $.Values.portLevelSetting | nindent 8 }}
---
{{ end }}
{{ end }}